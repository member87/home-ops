---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: headscale-admin
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/member87/home-ops.git
    targetRevision: HEAD
    path: apps/headscale-admin
  destination:
    server: https://kubernetes.default.svc
    namespace: headscale-admin
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
