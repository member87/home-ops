---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rook-nfs
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/member87/home-ops.git
    targetRevision: HEAD
    path: infrastructure/rook-nfs
  destination:
    server: https://kubernetes.default.svc
    namespace: rook-nfs
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
