---
# Media data PVC for Bazarr (shared media storage)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  namespace: bazarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Gi
