---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cdn-data
  namespace: cdn
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
