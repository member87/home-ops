---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cdn-data
  namespace: cdn
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
