---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cdn-data
  namespace: cdn
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
