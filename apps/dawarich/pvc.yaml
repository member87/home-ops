---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent   # bound PVC specs are immutable;
    # Flux inherited spec.volumeName from Helm's last-applied on some of these, and a
    # server-side apply that omits it then tries to clear it. Create-only avoids that.
  name: dawarich-public
  namespace: dawarich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent   # bound PVC specs are immutable;
    # Flux inherited spec.volumeName from Helm's last-applied on some of these, and a
    # server-side apply that omits it then tries to clear it. Create-only avoids that.
  name: dawarich-storage
  namespace: dawarich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent   # bound PVC specs are immutable;
    # Flux inherited spec.volumeName from Helm's last-applied on some of these, and a
    # server-side apply that omits it then tries to clear it. Create-only avoids that.
  name: dawarich-watched
  namespace: dawarich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent   # bound PVC specs are immutable;
    # Flux inherited spec.volumeName from Helm's last-applied on some of these, and a
    # server-side apply that omits it then tries to clear it. Create-only avoids that.
  name: dawarich-db-data
  namespace: dawarich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent   # bound PVC specs are immutable;
    # Flux inherited spec.volumeName from Helm's last-applied on some of these, and a
    # server-side apply that omits it then tries to clear it. Create-only avoids that.
  name: dawarich-redis-data
  namespace: dawarich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
