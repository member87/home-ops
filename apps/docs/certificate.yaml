---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: docs-cert
  namespace: docs
spec:
  secretName: docs-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - docs.lab.jackhumes.com
