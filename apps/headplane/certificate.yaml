---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headplane-tls
  namespace: headplane
spec:
  secretName: headplane-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - headplane.lab.jackhumes.com
