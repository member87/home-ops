---
# Media data PVC for Jellyfin (shared media storage)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  namespace: jellyfin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Gi
