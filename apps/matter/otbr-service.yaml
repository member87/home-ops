---
apiVersion: v1
kind: Service
metadata:
  name: otbr
  namespace: matter
  labels:
    app: otbr
spec:
  type: ClusterIP
  selector:
    app: otbr
  ports:
    - name: rest
      port: 8081
      targetPort: rest
      protocol: TCP
