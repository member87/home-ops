---
# Media data PVC for Radarr (shared media storage)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  namespace: radarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Gi
