---
# Media data PVC for Sonarr (shared media storage)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  namespace: sonarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Gi
