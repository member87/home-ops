---
# SealedSecret for ArgoCD Repository
# This file contains encrypted secrets that can only be decrypted by the sealed-secrets controller in your cluster
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: home-ops-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
spec:
  encryptedData:
    # These are placeholder values - replace with your actual sealed values
    # Generate with: echo -n 'your-secret-value' | kubeseal --raw --from-file=/dev/stdin --namespace argocd --name home-ops-repo
    type: REPLACE_WITH_SEALED_VALUE
    url: REPLACE_WITH_SEALED_VALUE
    # Uncomment and seal these for private repositories:
    # username: REPLACE_WITH_SEALED_VALUE
    # password: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: home-ops-repo
      namespace: argocd
      labels:
        argocd.argoproj.io/secret-type: repository
    type: Opaque
